##########
# A2A SDK – Unified Requirements File
#
# This file intentionally lists ALL dependencies (runtime + optional features + dev tools)
# so a single command `pip install -r requirements.txt` prepares a full dev environment.
# For lean installs, edit or create a slimmer runtime-only file.
##########

# --- Core runtime ---
httpx>=0.28.1
httpx-sse>=0.4.0
pydantic>=2.11.3
protobuf>=5.29.5
google-api-core>=1.26.0

# --- HTTP / Server (JSON-RPC & REST) ---
fastapi>=0.115.2
sse-starlette
starlette
uvicorn>=0.35.0

# --- gRPC support ---
grpcio>=1.60
grpcio-tools>=1.60
grpcio_reflection>=1.7.0

# --- Encryption ---
cryptography>=43.0.0

# --- Telemetry / Observability ---
opentelemetry-api>=1.33.0
opentelemetry-sdk>=1.33.0

# --- Databases (choose the driver(s) you need) ---
sqlalchemy[asyncio,postgresql-asyncpg]>=2.0.0
sqlalchemy[asyncio,aiomysql]>=2.0.0
sqlalchemy[asyncio,aiosqlite]>=2.0.0

# --- Development / Tooling ---
datamodel-code-generator>=0.30.0
mypy>=1.15.0
pytest>=8.3.5
pytest-asyncio>=0.26.0
pytest-cov>=6.1.1
pytest-mock>=3.14.0
respx>=0.20.2
ruff>=0.12.8
uv-dynamic-versioning>=0.8.2
types-protobuf
types-requests
pre-commit
pyupgrade
autoflake
no_implicit_optional
trio

# NOTE: Editable install remains recommended for iterative dev:
#   pip install -e '.[dev]'
